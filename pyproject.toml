[project]
name = "timematchai"
version = "0.1.0"
description = "TimeMatch — AI-driven class scheduling for universities. Landing + demo cabinet."
requires-python = ">=3.13,<3.14"
dependencies = [
    "django>=5.0,<6",
    "python-dotenv>=1.0",
]

[tool.uv]
package = false
